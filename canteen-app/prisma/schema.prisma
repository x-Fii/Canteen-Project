datasource db {
  provider = "sqlite"
  // url line is GONE
}

generator client {
  provider = "prisma-client-js"
}

model MenuItem {
  id           Int      @id @default(autoincrement())
  name         String
  price        Float
  category     String
  canteenLevel String
  createdAt    DateTime @default(now())
}